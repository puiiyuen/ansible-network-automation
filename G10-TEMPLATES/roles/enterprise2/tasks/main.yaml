# --------------------------------------
# Tasks -> main.yaml
# Role: enterprise2
# Create at Tue Jul 13 18:42:01 EDT 2021
# --------------------------------------

---
# - name: Configure Routers
#   cisco.ios.ios_config:
#     src: "config.j2"
#   with_items: "{{ routers }}"
#   when: 
#    - item.router_mgnt == inventory_hostname 

- name: Generate Configuration
  template:
    dest: /etc/ansible/G10-CONFIGS/enterprise2/{{item.hostname}}.txt
    src: "config.j2"
  with_items: "{{ routers }}"
  when:
    - item.router_mgnt == inventory_hostname