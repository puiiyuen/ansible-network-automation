---
- name: 
  gather_facts: false
  hosts: g10
  tasks: 
    - name: show run 
      cisco.ios.ios_command:
        commands:
          - show run
    
      register: print_output
    - debug: var=print_output.stdout_lines[0]
