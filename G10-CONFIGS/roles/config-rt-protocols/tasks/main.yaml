# --------------------------------------
# Tasks -> main.yaml
# Role: config-rt-protocols
# Create at Thu 15 Jul 2021 20:40:43 EDT
# --------------------------------------

---
# - name: Configure Routers
#   cisco.ios.ios_config:
#     src: "config.j2"
#   with_items: "{{ routers }}"
#   when: 
#    - item.router_mgnt == inventory_hostname 

- name: Generate Configuration
  template:
    dest: "G10-OUTPUTS/config-rt-protocols/{{item.hostname}}.txt"
    src: "config.j2"
  with_items: "{{ routers }}"
  when:
    - item.router_mgnt == inventory_hostname